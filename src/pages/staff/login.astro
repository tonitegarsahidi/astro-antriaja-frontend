---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="Login Petugas Loket - AntriAja" description="Masuk ke Konsol Staf Loket AntriAja">
  <div class="min-h-screen bg-slate-50 flex flex-col justify-center py-12 sm:px-6 lg:px-8">
    <div class="sm:mx-auto sm:w-full sm:max-w-md text-center">
      <!-- Logo -->
      <a href="/" class="inline-flex items-center space-x-2 text-blue-600 font-black text-2xl tracking-tight mb-4">
        <svg class="w-9 h-9" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M17 20H7C4.79086 20 3 18.2091 3 16V8C3 5.79086 4.79086 4 7 4H17C19.2091 4 21 5.79086 21 8V16C21 18.2091 19.2091 20 17 20Z" stroke="currentColor" stroke-width="2"/>
          <path d="M7 10L10 13L17 7" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <span>AntriAja</span>
      </a>

      <h2 class="text-2xl sm:text-3xl font-extrabold text-slate-900 tracking-tight">
        Login Petugas Loket
      </h2>
      <p class="mt-2 text-sm text-slate-600">
        Masuk untuk mengoperasikan antrian meja pelayanan loket Anda
      </p>
    </div>

    <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md px-4 sm:px-0">
      <div class="bg-white py-8 px-6 shadow-sm sm:rounded-2xl border border-slate-200 sm:px-10">
        <!-- Error Alert Container -->
        <div id="login-error-alert" class="hidden mb-6 p-4 rounded-xl bg-rose-50 border border-rose-200 text-sm text-rose-700 font-medium"></div>

        <form id="staff-login-form" class="space-y-5">
          <!-- Tenant Slug -->
          <div>
            <label for="input-tenant-slug" class="block text-xs font-semibold text-slate-700 uppercase tracking-wider mb-1.5">
              Kode / Slug Instansi
            </label>
            <div class="relative">
              <input
                id="input-tenant-slug"
                name="tenant_slug"
                type="text"
                required
                placeholder="misal: demo-bank"
                class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl text-slate-800 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:bg-white transition-all font-mono"
              />
            </div>
            <p class="mt-1 text-[11px] text-slate-400">Hubungi administrator cabang jika belum mengetahui slug instansi.</p>
          </div>

          <!-- Email -->
          <div>
            <label for="input-email" class="block text-xs font-semibold text-slate-700 uppercase tracking-wider mb-1.5">
              Alamat Email Petugas
            </label>
            <div class="relative">
              <input
                id="input-email"
                name="email"
                type="email"
                required
                placeholder="nama@instansi.com"
                class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl text-slate-800 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:bg-white transition-all"
              />
            </div>
          </div>

          <!-- Password -->
          <div>
            <label for="input-password" class="block text-xs font-semibold text-slate-700 uppercase tracking-wider mb-1.5">
              Kata Sandi
            </label>
            <div class="relative">
              <input
                id="input-password"
                name="password"
                type="password"
                required
                placeholder="Minimal 8 karakter"
                class="w-full px-4 py-2.5 bg-slate-50 border border-slate-200 rounded-xl text-slate-800 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:bg-white transition-all"
              />
            </div>
          </div>

          <!-- Submit Button -->
          <div class="pt-2">
            <button
              id="btn-login-submit"
              type="submit"
              class="w-full flex items-center justify-center py-3 px-4 rounded-xl text-sm font-bold text-white bg-blue-600 hover:bg-blue-700 shadow-sm transition-all focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 cursor-pointer disabled:opacity-50 disabled:cursor-not-allowed"
            >
              <span id="btn-login-text">Masuk ke Konsol</span>
              <svg id="btn-login-spinner" class="hidden w-5 h-5 ml-2 animate-spin text-white" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
              </svg>
            </button>
          </div>
        </form>

        <div class="mt-6 text-center">
          <a href="/" class="text-xs font-semibold text-slate-500 hover:text-blue-600 transition-colors">
            &larr; Kembali ke Portal Utama
          </a>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>

<script>
  import { login } from '../../services/authService';
  import { setToken, setTenantSlug, setUser, getTenantSlug } from '../../lib/storage';

  function initLoginPage() {
    const form = document.getElementById('staff-login-form') as HTMLFormElement | null;
    const slugInput = document.getElementById('input-tenant-slug') as HTMLInputElement | null;
    const emailInput = document.getElementById('input-email') as HTMLInputElement | null;
    const passwordInput = document.getElementById('input-password') as HTMLInputElement | null;
    const submitBtn = document.getElementById('btn-login-submit') as HTMLButtonElement | null;
    const btnText = document.getElementById('btn-login-text');
    const btnSpinner = document.getElementById('btn-login-spinner');
    const errorAlert = document.getElementById('login-error-alert');

    if (!form || !slugInput || !emailInput || !passwordInput || !submitBtn) return;

    // Pre-fill slug from URL query or localStorage
    const urlParams = new URLSearchParams(window.location.search);
    const querySlug = urlParams.get('slug') || getTenantSlug();
    if (querySlug) {
      slugInput.value = querySlug;
    }

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      if (errorAlert) {
        errorAlert.classList.add('hidden');
        errorAlert.textContent = '';
      }

      const tenant_slug = slugInput.value.trim();
      const email = emailInput.value.trim();
      const password = passwordInput.value;

      if (!tenant_slug || !email || !password) {
        if (errorAlert) {
          errorAlert.textContent = 'Harap lengkapi seluruh kolom isian';
          errorAlert.classList.remove('hidden');
        }
        return;
      }

      // Set loading state
      submitBtn.disabled = true;
      if (btnText) btnText.textContent = 'Memverifikasi...';
      if (btnSpinner) btnSpinner.classList.remove('hidden');

      try {
        const res = await login({ tenant_slug, email, password });

        if (!res.success || !res.data) {
          const errorMsg = res.error?.message || 'Login gagal. Periksa kembali email dan password Anda.';
          if (errorAlert) {
            errorAlert.textContent = errorMsg;
            errorAlert.classList.remove('hidden');
          }
          return;
        }

        // Save session credentials
        setToken(res.data.token);
        setTenantSlug(res.data.tenant.slug);
        setUser(res.data.user);

        // Redirect to staff console
        window.location.href = '/staff';
      } catch (err: unknown) {
        if (errorAlert) {
          errorAlert.textContent = 'Terjadi gangguan jaringan saat menghubungi server.';
          errorAlert.classList.remove('hidden');
        }
      } finally {
        submitBtn.disabled = false;
        if (btnText) btnText.textContent = 'Masuk ke Konsol';
        if (btnSpinner) btnSpinner.classList.add('hidden');
      }
    });
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initLoginPage);
  } else {
    initLoginPage();
  }
</script>
